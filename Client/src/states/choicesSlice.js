import { createSlice } from "@reduxjs/toolkit";

const initialState={
    AllChoices:[]
}
const choicesSlices = createSlice({

    name:"AllChoices",
    initialState,
    reducers:{
        
    }

})